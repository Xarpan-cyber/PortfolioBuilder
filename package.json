{
  "name": "portfolio-deployment",
  "version": "1.0.0",
  "description": "Root package.json for Railway deployment",
  "scripts": {
    "postinstall": "cd portfolio-builder && npm install",
    "start": "cd portfolio-builder && npm start"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "crypto": "^1.0.1",
    "nodemailer": "^9.0.5"
  }
}

